/**
 * ParcelasAtrasadasJob — chamar serviço de finanças para atualizar status de parcelas atrasadas.
 * Agendado diariamente pelo scheduler. Implementação real: importar atualizarParcelasAtrasadas e chamar por escola.
 */
export async function runParcelasAtrasadasJob(): Promise<void> {
  // Placeholder: em produção importar e chamar serviço de parcelas por escola.
  await Promise.resolve()
}
