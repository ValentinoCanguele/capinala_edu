/**
 * BoletinsBatchJob — pré-calcular boletins ou cache para relatórios pesados em horário de baixo uso.
 */
export async function runBoletinsBatchJob(): Promise<void> {
  // Placeholder: iterar escolas, opcionalmente pré-calcular boletins ou aquecer cache.
  await Promise.resolve()
}
