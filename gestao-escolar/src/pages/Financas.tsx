import { Outlet } from 'react-router-dom'

/**
 * Layout do módulo Finanças: sub-rotas (Visão geral, Categorias, etc.) renderizam no Outlet.
 */
export default function Financas() {
  return <Outlet />
}
