# Apenas o estritamente necessário — todo o resto do projeto é incluído no upload

# Dependências (instaláveis com npm install; muito grandes)
node_modules/

# Segredos — NUNCA commitar
.env
.env.local
.env.*.local

# Build Next.js (regenerável)
.next/
